# 🚀 Инструкция по деплою Real Estate Platform

## 📋 Предварительные требования

- Docker 20.10+
- Docker Compose 2.0+
- Доступ к серверу через SSH
- Домен настроен на IP сервера
- S3-совместимое хранилище (Yandex Object Storage, AWS S3 и т.д.)

---

## 🔧 Шаг 1: Настройка окружения

### 1.1 Клонирование репозитория

```bash
git clone <your-repo-url> real-estate-platform
cd real-estate-platform
```

### 1.2 Создание .env файла

```bash
cp .env.example .env
nano .env  # или используйте vim, vi и т.д.
```

### 1.3 Заполнение переменных окружения

Обязательно измените следующие значения:

```env
# Домен вашего сайта
DOMAIN=propertyinterest.ru
PORT=80

# База данных - ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ ПАРОЛИ!
DB_NAME=realestate
DB_USER=realestate_user
DB_PASSWORD=СГЕНЕРИРУЙТЕ_СИЛЬНЫЙ_ПАРОЛЬ
DB_ROOT_PASSWORD=СГЕНЕРИРУЙТЕ_ДРУГОЙ_СИЛЬНЫЙ_ПАРОЛЬ

# Генерация паролей:
# openssl rand -base64 32

# S3 хранилище (для загрузки изображений)
S3_ENDPOINT=https://storage.yandexcloud.net
S3_BUCKET_NAME=ваш-bucket
S3_ACCESS_KEY=ваш-access-key
S3_SECRET_KEY=ваш-secret-key
S3_REGION=ru-central1

# JWT секрет - ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!
JWT_SECRET=$(openssl rand -base64 64)

# Пароль администратора - ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!
ADMIN_PASSWORD=$(openssl rand -base64 24)

# Информация о сайте
NEXT_PUBLIC_SITE_NAME="PropertyInterest.ru"
NEXT_PUBLIC_SITE_DESCRIPTION="Недвижимость в Москве"
NEXT_PUBLIC_API_URL=http://propertyinterest.ru/api
```

---

## 🐳 Шаг 2: Сборка и запуск

### 2.1 Сборка Docker образа

```bash
docker compose build
```

### 2.2 Запуск приложения

```bash
docker compose up -d
```

### 2.3 Проверка статуса

```bash
# Проверить работающие контейнеры
docker compose ps

# Должны увидеть:
# real-estate-app    running
# real-estate-mysql  running
```

---

## 🗄️ Шаг 3: Инициализация базы данных

### 3.1 Дождитесь готовности приложения

```bash
# Мониторинг логов
docker compose logs -f app

# Дождитесь сообщения: "Ready started server on 0.0.0.0:3000"
```

### 3.2 Инициализация БД

```bash
curl -X POST http://localhost/api/admin/init
```

Или откройте в браузере: `http://your-domain/api/admin/init`

---

## ✅ Шаг 4: Проверка работоспособности

### 4.1 Проверка API

```bash
# Health check
curl http://localhost/api/auth/check

# Должен вернуть: {"authenticated":false}
```

### 4.2 Проверка авторизации

```bash
curl -X POST http://localhost/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"password":"ВАШ_ADMIN_PASSWORD"}'
```

### 4.3 Открытие в браузере

```bash
# Откройте в браузере
http://your-domain
```

---

## 🔄 Обновление приложения

### Стандартное обновление

```bash
# 1. Получить последние изменения
git pull

# 2. Пересобрать образ
docker compose build

# 3. Перезапустить с downtime
docker compose down
docker compose up -d
```

### Zero-downtime обновление (рекомендуется)

```bash
# 1. Получить последние изменения
git pull

# 2. Пересобрать образ
docker compose build

# 3. Запустить новый контейнер
docker compose up -d --no-deps --build app

# Docker автоматически выполнит rolling update
```

---

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Все сервисы
docker compose logs -f

# Только приложение
docker compose logs -f app

# Только база данных
docker compose logs -f mysql

# Последние 100 строк
docker compose logs --tail=100 app
```

### Проверка использования ресурсов

```bash
docker stats
```

---

## 🛠️ Управление

### Остановка приложения

```bash
docker compose stop
```

### Полное удаление (с данными БД!)

```bash
docker compose down -v
# ВНИМАНИЕ: флаг -v удаляет volumes с данными!
```

### Перезапуск без пересборки

```bash
docker compose restart
```

### Резервное копирование БД

```bash
# Создание backup
docker compose exec mysql mysqldump -u root -p${DB_ROOT_PASSWORD} realestate > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление из backup
docker compose exec -T mysql mysql -u root -p${DB_ROOT_PASSWORD} realestate < backup_20240101_120000.sql
```

---

## 🔒 Безопасность

### Рекомендации для production

1. **Используйте обратный прокси (nginx/caddy) перед Docker**
   - SSL/TLS сертификаты
   - Rate limiting
   - DDoS защита

2. **Настройте firewall**
   ```bash
   # Пример для ufw
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw enable
   ```

3. **Регулярно обновляйте образы**
   ```bash
   docker compose pull
   docker compose up -d
   ```

4. **Используйте сильные пароли**
   - Минимум 32 символа для JWT_SECRET
   - Минимум 24 символа для паролей БД

5. **Ограничьте доступ к БД**
   - В production НЕ открывайте порт 3306 наружу
   - Используйте только внутреннюю сеть Docker

---

## 🐛 Решение проблем

### Приложение не запускается

```bash
# Проверить логи
docker compose logs app

# Проверить переменные окружения
docker compose exec app env | grep DB_
```

### Ошибка подключения к БД

```bash
# Проверить статус MySQL
docker compose exec mysql mysqladmin -u root -p${DB_ROOT_PASSWORD} ping

# Проверить сеть
docker network inspect real-estate-platform_app-network
```

### Порт уже занят

```bash
# Изменить порт в .env
PORT=8080

# Перезапустить
docker compose up -d
```

### Очистка всех Docker ресурсов (ОСТОРОЖНО!)

```bash
# Остановить все контейнеры проекта
docker compose down

# Удалить неиспользуемые образы
docker image prune -a

# Удалить неиспользуемые volumes (ПОТЕРЯ ДАННЫХ!)
docker volume prune
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker compose logs -f`
2. Проверьте .env файл
3. Убедитесь что все порты свободны
4. Проверьте что Docker daemon запущен

---

## 📚 Полезные команды

```bash
# Войти в контейнер приложения
docker compose exec app sh

# Войти в MySQL
docker compose exec mysql mysql -u root -p${DB_ROOT_PASSWORD} realestate

# Проверить версию Node.js в контейнере
docker compose exec app node --version

# Проверить использование места
docker compose exec app du -sh /app

# Пересоздать только один сервис
docker compose up -d --force-recreate app
```

---

## ✨ Готово!

Ваше приложение развёрнуто и готово к использованию! 🎉

Не забудьте:
- ✅ Настроить SSL сертификаты
- ✅ Настроить backup базы данных
- ✅ Настроить мониторинг
- ✅ Протестировать все функции

